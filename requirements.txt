# Core web framework
Flask>=3.1.0

# .env loader
python-dotenv>=1.0

# HTTP client (สำหรับเรียก Ollama API)
httpx>=0.28

# OCR / Document processing
docling>=2.91.0           # Layout + table structure + OCR pipeline
docling-core>=2.74.0
docling-ibm-models>=3.13.0
docling-parse>=5.10.0

# OCR backend — Apple Vision (macOS only; pip auto-skips on other OS)
ocrmac>=1.0.1; platform_system == "Darwin"

# OCR backend — EasyOCR (cross-platform fallback; PyTorch-based)
easyocr>=1.7.1

# Manga / vertical Japanese pipeline (comic-text-detector + manga-ocr)
mokuro>=0.2.4
manga-ocr>=0.1.14

# Image utilities
Pillow>=11.0
opencv-python>=4.10

# (Optional) Anthropic SDK สำหรับ Claude API correction — ปัจจุบันยังไม่ใช้ แต่ติดไว้เผื่อ
anthropic>=0.97.0

# Gemini SDK — สำหรับ batch translation/correction engine
google-genai>=0.3.0

# Vector search สำหรับ Translation Memory (TM) suggest
# Embeddings ผ่าน Ollama (nomic-embed-text) — ไม่ต้องเพิ่ม SDK
faiss-cpu>=1.7.4
numpy>=1.26

# NLLB-200 local translation engine (PC fallback แทน Apple Translate)
transformers>=4.40
sentencepiece>=0.2.0

# Pre-flight RAM check — กัน process ถูก OS kill ระหว่าง OCR (silent OOM)
psutil>=5.9
